<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, initial-scale=1.0">
    <title>로그인</title>

    <style>
        @import url("https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable.min.css");

        @font-face {
            font-family: 'AJou';
            src: url(AjouTTF.ttf) format('truetype');
            font-weight: normal;
            font-style: normal;
        }


        * {
            font-family: "Pretendard Variable";
        }

        html,
        body {
            padding: 0;
            margin: 0;
            overflow-x: hidden;
        }

        body::-webkit-scrollbar {
            width: 10px;
            background-color: white;
        }

        body::-webkit-scrollbar-thumb {
            width: 10px;
            background-color: #2A64AD;
            border-radius: 20px;
        }

        html {
            scroll-behavior: smooth;
        }


        main {
            width: 100%;
            height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .loginBox {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .loginBox>.formLine {}

        .loginBox>.formLine>form {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 1rem;
        }

        .loginBox>.formLine>form>.inputLine {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .loginBox>.formLine>form>.inputLine>input {
            width: 200px;
            height: 30px;
            border-radius: 5px;
            border: 1px solid black;
            padding: 10px 20px;
        }

        .loginBox>.formLine>form>.submitLine {}

        .loginBox>.formLine>form>.submitLine>button {
            width: 150px;
            height: 50px;
            border: none;
            background-color: #2A64AD;
            cursor: pointer;
            transition: all 0.3s;
            color: white;
            border-radius: 50px;
        }

        .loginBox>.formLine>form>.submitLine>button:hover {
            background-color: #427fc8;
        }
    </style>
</head>

<body>
    <main>
        <div class="loginBox">
            <div class="formLine">
                <form action="/auth"
                      method="post">
                    <div class="inputLine">
                        <input type="text"
                               name="admin_id"
                               placeholder="아이디">
                        <input type="password"
                               name="admin_pw"
                               placeholder="비밀번호">
                    </div>
                    <div class="submitLine">
                        <button>로그인</button>
                    </div>
                </form>
            </div>
        </div>
    </main>


    <script>
                console.log(`%c
 ,_     _
 \|\\\\_,-~\/
 \/ _  _ |    ,--.
(  @  @ )   \/ ,-'
 \\  _T_\/-._( (
 /         \`. \\
|         _  \\ \|
 \\ \\ ,  /      \|
  \|\| \|-_\\__   \/
 ((_/\`(____,-'
 `, 'color: gray; font-weight: bold;')

 console.log(`%c이걸 찾아?? 👍👍`, "color: orange; background-color: blue;padding: 4px; border-radius: 4px; font-size:32px")
    </script>
</body>

</html>